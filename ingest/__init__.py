"""Source adapters: one module (or subpackage) per source, each declaring its
source class and target stores. Shared machinery lives in `ingest.base`.

Run with `python -m ingest list | run NAME | canary [NAME ...]`.
"""
