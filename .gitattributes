# Code runs in Linux containers: keep LF on every checkout, including Windows.
* text=auto eol=lf
*.ps1 text eol=crlf
*.bat text eol=crlf
# Recorded source responses: byte-exact, checked against archive digests.
tests/fixtures/** -text
